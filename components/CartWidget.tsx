"use client";
import React, { useEffect } from "react";
import { useCart } from "./CartContext";
import { ShoppingCart } from "lucide-react";

// CartWidget is now disabled and does not render anything
export default function CartWidget() {
  return null;
}
 